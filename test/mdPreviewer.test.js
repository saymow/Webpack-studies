import mdPreviewer from "../src/mdPreviewer";

describe("markdownPreviewer", () => {
  it("should exist", () => {
    expect(mdPreviewer).toBeDefined();
  });
  it("shoud be equal", () => {
    expect(5).toBe(5);
  });
});
